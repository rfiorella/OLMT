name: Docker Image CI

on:
  push:
    branches: [ "rfiorella/era5" ]
  pull_request:
    branches: [ "rfiorella/era5" ]

jobs:

  build:

    runs-on: ubuntu-latest
    container:
      image: fasstsimulation/elm_v2-for-ngee_multiarch
      volumes: inputdata
    steps:
    - name: Checkout repo
      uses: actions/checkout@v3
    - name: Download inputdata
      run: ./scripts/download_elm_singlesite_forcing_data.sh
